import { Text, View } from 'react-native';

const PlannerScreen = () => {
    return (
        <View className='bg-background flex-1 items-center justify-center'>
            <Text className='text-muted-foreground text-2xl'>PLANNER</Text>
        </View>
    );
};

export default PlannerScreen;